This folder is a ROS1 generated package for the robot arm URDF.
However, the files inside should be good to use in ROS2 packages (ie the arm SRDF file).
This excludes package files like the xml and cmake files.

